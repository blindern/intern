<p>Denne brukerlisten kan ses av alle innloggede brukere. Totalt eksisterer det {{user_count}} brukere i systemet.</p>

{{#each sections}}
	<div class="panel {{paneltag}} userlist">
		<div class="panel-heading">{{title}} ({{users.length}} stk)</div>
		<table class="table table-striped table-hover table-condensed">
			<thead>
				<tr>
					<th>Brukernavn</th>
					<th>Navn</th>
					<th>E-post</th>
					<th>Telefon</th>
					<th>Grupper</th>
				</tr>
			</thead>
			<tbody>
				{{#each users}}
					<tr>
						<td><a href="/intern/user/{{username}}">{{username}}</a></td>
						<td>{{realname}}</td>
						{{#if ../../hidemail}}
							<td><i>Skjult</i></td>
						{{else}}{{#if email}}
							<td>{{email}}</td>
						{{else}}
							<td class="text-muted"><i>Ukjent</i></td>
						{{/if}}{{/if}}
						<td>
							{{#if phone}}{{phone}}
							{{else}}&nbsp;{{/if}}
						</td>
						<td>
							{{#each groups}}
								<a href="/intern/group/{{this}}">{{this}}</a><!--
								-->{{#unless @last}},{{/unless}}
							{{else}}
								<i class="text-muted">Ingen grupper</i>
							{{/each}}
						</td>
					</tr>
				{{/each}}
			</tbody>
		</table>
	</div>
{{/each}}